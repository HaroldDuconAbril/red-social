# Dependencias
node_modules/

# Variables de entorno (NUNCA subir esto a GitHub)
.env
.env.local
.env.*.local

# Fotos guardadas localmente (ya usamos Cloudinary, no hace falta subirlas)
Backend/uploads/

# Build de producción del frontend
frontend/dist/
frontend/build/

# Logs
*.log
npm-debug.log*

# Sistema operativo / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Backups de base de datos (contienen datos sensibles, nunca subir)
backup.sql
*.sql
!Backend/migrations/*.sql